# Copy this file to .env.local for local development with a live backend.
# In production, set VITE_API_URL as a repository secret in GitHub Actions.
# See .github/workflows/deploy.yml for where to uncomment the env block.

# Base URL of the deployed Express backend (no trailing slash).
# Example: https://your-app.up.railway.app
VITE_API_URL=
